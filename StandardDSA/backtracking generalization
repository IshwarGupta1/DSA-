void solve(&v)--> v indicates to variables in which action or choice will be done, they need to be passed by reference
{
  if(isSolved())---> isSolved() is the base functionality which meets the stopping condition
  {
    print/save
    return
  }
  for(c in choices)
  {
    if(isvalid)-->meets satisfactory criteria
    {
      change in v
      solve(v)
      revert change in v
    }
  }
return;
}
