// variables data types & structure
var description = "We need a concrete floor";
var sqMtrs = 100;
let floorcolor = "brown is color"; // we can override
const floorPerim = 500; // when we use const it cannot be override later
const colorOrNot = true;
const floorOptions = new ['red', 'blue', 1000, 43.5, false, ['lol']];
const obj1 = new{
    Name : 'John',
    Home : 'Bellandur',
    Age : 50
};
var ownerAge = obj1.Age;
